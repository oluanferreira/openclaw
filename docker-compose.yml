name: clawin1click-openclaw

services:
  db:
    image: postgres:18-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-clawin1click}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-clawin1click}
      POSTGRES_DB: ${POSTGRES_DB:-openclaw}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD",
          "pg_isready",
          "-U",
          "${POSTGRES_USER:-clawin1click}",
          "-d",
          "${POSTGRES_DB:-openclaw}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
